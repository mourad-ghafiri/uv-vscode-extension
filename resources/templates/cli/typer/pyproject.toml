[project]
name = "{{PROJECT_NAME}}"
version = "0.1.0"
description = "A CLI application built with Typer"
readme = "README.md"
requires-python = ">={{PYTHON_VERSION}}"
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]
