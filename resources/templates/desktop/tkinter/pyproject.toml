[project]
name = "{{PROJECT_NAME}}"
version = "0.1.0"
description = "A Tkinter desktop application"
readme = "README.md"
requires-python = ">={{PYTHON_VERSION}}"
dependencies = [
    "ttkbootstrap>=1.10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pyinstaller>=6.0.0",
]
